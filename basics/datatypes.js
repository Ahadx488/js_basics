//"use strict" enforces strict mode in JavaScript to prevent common coding errors and improve code reliability.

"use strict"; // treat all JS code as newer version

/*Use of      "alert()"      in JavaScript

alert() is used to display a pop-up message box to the user with an OK button.

Why it is used

To show information or warnings

To debug code (quickly check values)

To get user attention before continuing*/

// data-types

// number => 2 to the power 53
// bigint
// string => ""
// null => standalone value
// boolean => true / false
// undefined => value is not assigned yet
// symbol => used for uniqueness (mainly in react)
// object

console.log( typeof undefined) // o/p => undefined
console.log(typeof null ) // o/p => null



